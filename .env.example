# Optional. If unset, app uses JSON file storage (data/contacts.json).
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional. Defaults to "080205".
# ADMIN_PASSWORD=changeme

# Optional. Custom data directory for the JSON fallback (e.g. mounted volume on Railway).
# DATA_DIR=/data
